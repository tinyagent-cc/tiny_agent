## What this does and why

## Checklist

- [ ] `ctest --preset default` is green (301 offline cases, no keys, no network).
- [ ] If a provider, vector store, or observability adapter changed: ran its
      live test against the real backend and pasted the output below.
- [ ] Docs updated (`README.md`, `docs/*.md`) if this changes a public type or
      behavior a doc describes.

## Live test output (if an adapter changed)

```
paste here, or delete this section if nothing adapter-facing changed
```
